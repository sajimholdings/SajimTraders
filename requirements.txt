# Sajim Traders Production Requirements (Linux/Render-safe)
numpy>=1.26.0
requests>=2.31.0
urllib3>=2.0.0
python-dotenv>=1.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
supabase>=2.5.0
cryptography>=42.0.0

# NOTE: MetaTrader5 is a Windows-only package and is NOT installable on Linux/Render.
# Install it on the Windows bridge host only (see requirements-windows.txt).

